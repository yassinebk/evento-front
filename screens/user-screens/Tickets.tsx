import React from "react";
import { Paragraph } from "tamagui";

interface TicketsProps {}

export const Tickets: React.FC<TicketsProps> = ({}) => {
	return <Paragraph>Tickets</Paragraph>;
};

export default Tickets;
